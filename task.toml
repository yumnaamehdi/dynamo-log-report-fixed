artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an access log and generate a JSON summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = ["transform", "generate"]
artifact_type = ["json"]
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "The task requires parsing a server access log, counting requests, identifying unique client IPs, and determining the most frequently requested path."
solution_explanation = "Read /app/access.log, count all non-empty entries, count distinct client IP addresses, find the most frequently requested URL path, and write the results to /app/report.json."
verification_explanation = "The verifier checks that /app/report.json exists, is valid JSON, contains exactly the required fields, and matches the expected values derived from the provided log."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []
